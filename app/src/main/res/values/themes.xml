<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.QueriVerse" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/primary</item>
    </style>

    <style name="Theme.QueriVerse" parent="Base.Theme.QueriVerse" />
</resources>