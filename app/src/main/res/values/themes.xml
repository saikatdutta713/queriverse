<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QueriVerse" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.QueriVerse" parent="Base.Theme.QueriVerse" />

    <style name="AppBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <!-- Change background color here -->
        <item name="android:background">@color/primary</item>
        <!-- Change text color here -->
        <item name="android:textColor">@color/white</item>
        <!-- Change icon color here -->
        <item name="android:iconTint">@color/white</item>
    </style>

</resources>